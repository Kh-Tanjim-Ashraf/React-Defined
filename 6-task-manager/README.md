# Task Manager

The frontend consumes data from a task manager API server, initially it can handle CRUD operations on task. The tasks are initially displayed as a list under a task form. A user can create a new task by submitting the form & can see the task appeared in the list below.
